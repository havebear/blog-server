<html>
  <head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="/public/styles/style.css" />
  </head>
  <body>
    <div class="page-home">

    </div>
    <ul class="news-view view">
      {% for item in list %}
        <li class="item">
          <a href="{{ item.url }}">{{ item.title }}</a>
        </li>
      {% endfor %}
    </ul>
  </body>
</html>